PORT=5500

# Admin API protection for GET /api/leads
ADMIN_TOKEN=replace-with-strong-admin-token

# Optional SMTP setup for lead email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
NOTIFICATION_EMAIL=
